//In production should be loaded from env variables.

const keys = {
    host: 'localhost',
    port: 5432,
    user: 'postgres',
    password: 'Passw0rd1',
    database: 'UserProfile',
    max: 20,
};

module.exports = keys;